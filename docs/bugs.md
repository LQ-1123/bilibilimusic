# BiliMusic 真实 Bug 记录（生产实测）

> 只记录**用户真机实测确认**的问题：现象 → 根因（代码定位）→ 修复 → 验证。
> 每条都对应一次可复现的线上行为，不是推测。

---

## BUG-001 收藏夹同步只拿到子集（多设备数量各不相同）｜ P0 ｜ v1.0.1 已修

**现象**：同一 B 站账号在多台设备登录，App 曲库数量各不相同（用户实测 11 / 18），而 B 站 `bilimusic` 收藏夹实际有 **30** 个视频。

**根因**：`app/bili/client.py` 的 `get_fav_videos()` 用响应里的 `media_count` 当翻页终止条件：

```python
total = int(data.get("media_count") or 0)      # 该接口顶层没有 media_count → 恒为 0
...
if not medias or (total is not None and len(out) >= total): break   # 20 >= 0 → 第一页就 break
```

`/x/v3/fav/resource/list` 把 `media_count` 放在 `data.info` 里，**顶层为 None**，于是 `total=0`，循环永远只翻第一页（ps=20）。收藏夹按收藏时间排序，新收藏会把旧视频挤出前 20 —— 所以每台设备各自停在「当时的前 20 条」，表现为不同子集。

**修复**：终止条件改用响应里的 **`has_more`**；页数上限由 `cap` 计算（不再硬编码 50 页）。`submit_fav`（收藏夹链接批量导入）走同一函数，一并修复。

**验证**（用用户账号真实 cookie 实测）：

| 收藏夹 | 修复前 | 修复后 |
|---|---|---|
| `bilimusic`（30 个） | 20 ❌ | **30** ✅ |
| `默认收藏夹`（28 个） | 20 ❌ | **28** ✅ |

用户真机确认：多设备曲库数量与 B 站收藏夹一致。

---

## BUG-002 移动端短信登录：极验滑块不渲染 ｜ P1 ｜ v1.0.0 修，v1.0.1 改为下线

**现象**：Android 上填手机号点「获取验证码」，极验验证窗口不出现（或出现但不可见），无法完成短信登录。

**根因**：`MainActivity` 的 WebView 只开了 `setJavaScriptEnabled` + `setDomStorageEnabled`，缺两样极验必需项：
1. 默认 UA 含 `; wv` → 被风控识别为 WebView，不下发滑块；
2. 跨站脚本（`static.geetest.com`）的第三方 Cookie 被拦 → 极验会话拿不到。

**修复**：伪装成手机 Chrome UA（`Mobile Safari/537.36`）、`setAcceptThirdPartyCookies(true)`、`setDatabaseEnabled(true)`、`setMixedContentMode(ALWAYS_ALLOW)`。页面布局走 CSS 视口断点，不受 UA 影响。

**后续**：短信登录体验仍不理想（依赖极验 CDN，跨境线路慢），v1.0.1 整体下线，改为二维码方案（见 FEAT-001）。

---

## BUG-003 验证参数接口偶发超时，前端只显示笼统失败 ｜ P1 ｜ v1.0.1 已修

**现象**：手机端点「获取验证码」直接提示「验证参数获取失败」，无法排查。

**根因**：`passport.bilibili.com/x/passport-login/captcha` 实测 **3 次里 1 次 ConnectTimeout**（经代理/VPN 更明显）；后端异常未转成可读响应，前端 `r.ok ? r.json() : null` 拿到 null，统一显示笼统文案。

**修复**：`captcha_get()` 网络类错误重试 3 次 + 单次超时 20s→5s（失败也快）；`/api/auth/captcha` 把 `BiliApiError`/`httpx` 错误转成 `502 + detail`；前端直接展示 detail。实测 5/5 成功、耗时 0.8~1.6s。

---

## BUG-004 曲库计数与 B 站收藏夹口径不一致 ｜ P1 ｜ v1.0.0 已修

**现象**：本地「我的曲库」显示 12，但 B 站收藏夹有 20+，歌单详情页又显示 13，三处对不上。

**根因**：本地计数是「已收藏的**歌曲**数」（`collected=1`），B 站是「收藏的**视频**数」；一个多 P 合集在 B 站算 1 条、在本地算 0~N 首（取决于是否逐曲收藏）。收藏后侧边栏还没刷新，进一步放大了偏差。

**修复**：计数改为**视频级**——收藏单曲 1 条 + 合集容器 1 条；曲库详情页新增「合集」货架，让计数与内容一致；收藏/移出时触发 `playlistsChanged` 刷新侧栏。

---

## BUG-005 推荐歌单卡片右下角多余播放按钮 ｜ P2 ｜ v1.0.1 已修

**现象**：推荐歌单卡片右下角悬浮一个白色圆形播放钮，视觉突兀。

**根因**：`partials/rec_playlists.html` 的 `<span class="fab">`。

**修复**：删除该元素（`partials/playlist_rack.html` 的「我的歌单」卡片上同款按钮保留，按需再定）。

---

## BUG-006 音频流按 id 取最大选档，放开高音质后会选错 ｜ P1 ｜ `[待修]`（用户要求先记不修）

**现象**：目前无感。一旦支持杜比/Hi-Res，会**挑到 192K 而不是 Hi-Res**。

**根因**：`app/bili/quality.py` 的 `pick_best_audio()` 用 `max(streams, key=lambda s: s.quality_id)`，
但 B 站音质 id 的**数字大小 ≠ 音质高低**：

| id | 档位 |
|---|---|
| 30216 | 64K |
| 30232 | 132K |
| 30280 | **192K** |
| 30250 | 杜比全景声 |
| 30251 | **Hi-Res 无损** |

`max(id)` 在所有档位里会选到 **30280（192K）**，把 30251（Hi-Res）/ 30250（杜比）排在后面。

**当前为何不炸**：`client.py` 请求 playurl 用的是 `fnval=16`（仅基础 DASH），B 站只返回
30216/30232/30280 三条 AAC 流，`max(id)` 恰好等于 192K —— 属于「巧合正确」。

**修复方向**（用户要求暂缓）：
1. 改为按 `bandwidth` 降序（同码率再用质量优先级 tie-break）——`AudioStream` 已经存了 `bandwidth`；
2. 若后续放开 `fnval=4048`，另需处理 `dash.flac.audio`（无损）与 `dash.dolby.audio[]`，
   优先 flac → dolby → dash.audio 按带宽取最高；
3. 注意 FLAC/Hi-Res 码率约 1000+ kbps，本服务是**实时代理不落地**，建议 Wi-Fi 优先无损、蜂窝自动降档。

**参考**：`wood3n/biu` 的 `src/common/utils/audio.ts`（`sortAudio` 主键 bandwidth 降序 +
`audioQualitySort` 优先级）与 `electron/ipc/api/audio-stream-url.ts`（带 Cookie/Referer/UA +
按会员状态选 quality）。

---

## FEAT-001 登录方式改为「点二维码 → 存相册 + 拉起 B 站 App」｜ v1.0.1

**背景**：短信登录依赖极验（见 BUG-002/003），体验差；扫码在单手机上又不方便（没法用同一台机器扫自己的屏幕）。

**方案**：登录弹窗改为**单列扫码**，点二维码后：
1. Android 经原生桥 `saveQrToGallery` 写入系统相册（API 29+ 走 scoped storage 免权限；≤28 申请 `WRITE_EXTERNAL_STORAGE`）；
2. `openBilibiliApp` 拉起 B 站 App；
3. 用户在 B 站 App「扫一扫 → 相册」选中该二维码即可确认登录，后端轮询到确认自动进入；
4. 浏览器/桌面回退为直接下载 PNG。

**前提**：B 站 App 的「扫一扫」需有相册入口（用户侧确认）。

---

## BUG-007 网易云错配被当成「命中」，不再降级到下一级歌词源｜ P1 ｜ 已修（未打包）

**现象**（用户实测）：部分歌拿到**完全不对**的歌词，来源角标却显示「网易云」，并且**不会继续尝试下一级**
（B 站 AI 字幕 / LRCLIB 纯文本）。用户判断：「网易云会识别到错误的歌词，但是也是识别到了，而不是转到下一级
——我觉得这个兜底机制有问题。」典型触发场景：周杰伦等歌手网易云无版权。

**根因**（`app/services/lyrics.py` → `_from_netease`，L307-322）：

1. **唯一门槛是时长，且时长未知时门槛消失**
   ```python
   if duration and duration_ms and abs(duration_ms / 1000 - duration) > _DURATION_TOLERANCE:
       continue
   ```
   `duration` 为 0（未解析到）时任何搜索结果都直接通过，无任何其他校验。

2. **搜索结果大多是翻唱/同名曲，时长恰好落进 ±3s 就采纳**。用真实接口实测（`Referer: music.163.com`）：
   | 查询 | 网易云返回（时长） |
   |---|---|
   | `周杰伦 晴天` | 全是翻唱：`晴天 (原唱 周杰伦) - RyaVocal` 270s、`晴天(深情版) - Lucky小爱` 278s、`晴天 (女声版))` 268s、`晴天 (钢琴版)` 238s、`晴天 - 梦里啥都有` 228s |
   | `稻香 周杰伦` | `稻香 - Montagem` 183s、`稻香(正式版) - 沈幼楚` 54s、`稻香 - 何兵兵` 121s、`稻香 - Lucky小爱` 231s、`稻香 - Lie` 196s |

   原唱《晴天》269s → `RyaVocal` 270s **命中 ±3s**；原唱《稻香》223s → 这批靠时长被挡掉，
   但 B 站视频若是 CUT/片段（时长接近 183s 或 231s）就会采纳 `Montagem` 那条——它的歌词是
   「稻香，稻香，稻香／那年夏天 我们一起／躺在草地上看星星」，**同名但完全另一首歌**。
   `沈幼楚` 那条 54s 是一段念白（「·通常大家认为 人和人的感情像水一样…」）。

3. **「带轴」判定可被全零时间戳骗过**
   ```python
   if lyric and _SYNCED_HEAD_RE.search(lyric[:200]):   # _SYNCED_HEAD_RE = \[\d{1,2}:\d{2}
   ```
   网易云歌词头部常带 `[00:00.000] 作词 : xxx`，前 200 字必然命中正则；`沈幼楚` 那条 12 行**全是**
   `[00:00.000]·…`，同样判为「带轴」——但所有行时间都是 0，前端 `parsed.timed = timed >= 2` 也成立，
   高亮会全部挤在 0 秒。

4. **首个可接受结果直接 return，不排序也不复核标题/歌手**。对比同文件 `pick_lrclib_result`
   （rank = 带轴优先 + 时长最接近优先），`_from_netease` 的 for 循环里是「第一个通过就返回」，
   且从不比对 `item["name"]` / `artists` 与候选歌名。

5. **链路命中即短路**（`fetch_for_song` L165-167）：错误的 ncm 结果会**挡住**后面的 B 站 AI 字幕
   （对翻唱视频而言 AI 字幕往往是**对的**）与 LRCLIB 纯文本。

**附带现象**：LRC 元数据行未被过滤。前端 `NOTE_RE` 只剥 `♪♫♩♬`（`app.js:754`），
所以 `晴天 (原唱 周杰伦)` 的歌词会把 `作词 : Chieh-lun Chou`、`作曲 : Chieh-lun Chou`、
`编曲 : 周杰伦` 当成歌词行显示在最前面。

**修复方向**（用户要求先攒台账，未开工）：

- **A. 置信度判定，不达标就降级而不是 return**
  - 时长未知 → 外部源一律要求歌名**完全相等**才采纳；
  - 采纳前复核返回歌名/歌手与候选的相似度（归一化后相等或包含）；
  - 拒绝退化时间轴（不同非零时间戳 < 3 个，或末行时间 < 时长 50% 且行数 > 6）；
  - 行数下限（≥8）+ 时间覆盖率下限。
- **B. 改「多源候选 + 打分选最优」**：各源产出（文本, 源, 分数），统一排序；最高分低于阈值即视为未找到，
  继续降级。这是用户诉求「错的不该挡住下一级」的结构性解法。
- **C. 无版权歌手的正确兜底**：网易云无版权时应该**拿不到**（现状是拿到错的）——
  真正该承担的是 LRCLIB（《晴天》《稻香》都有带轴）+ B 站 CC/AI 字幕 + 视频简介；
  再加手动搜索 + 歌词偏移（`wood3n/biu` 已有）。
- **D. `_SYNCED_HEAD_RE` 换成 `_looks_synced(lyric, duration)`** 做真实时间轴校验。
- **E. 元数据行清洗**：剥掉 `作词/作曲/编曲/OP/SP/制作人 :` 等头部行，前端与后端各一道。

**现有兜底**：歌词页来源角标（`#lyrics-source`）+ `↻` 重试按钮（`/api/songs/{id}/lyrics?force=1`）——
但重试会**再走同一条链路**，命中同一个错配结果，对用户没有帮助。

**实测铁证**（用户库中唯一一条 `ncm` 结果，song id 415）：

| 项 | 值 |
|---|---|
| B 站视频 | `【Rammstein】德国战车 MV精选（DVD） · Ich tu dir weh`（UP：david_-young，237s） |
| 网易云给的歌词 | `[00:00.00] 作词 : Jaymes Young/Billboard …` / `Baby this love` / `I'll never let it die` |
| 实际是 | **Jaymes Young《Infinity》**——另一首歌的歌词 |

另一条 `ai` 结果（song id 127，现场演出）内容是「出去吧／这是一只／♪ 音乐 ♪／宝宝／
有人想这么做吗」——AI 字幕当歌词同样不合格。即：**当前库里两条非 CC 结果全是坏的**。

### 修复（`app/services/lyrics.py`，未打包）

1. **置信度校验 `match_confidence()` / `accept_external()`**——外部源（LRCLIB / 网易云）命中项必须：
   - 双方时长已知且相差 ≤3s（**硬门槛**）；
   - 歌名归一化后相等或互相包含（**硬门槛**）；
   - 综合打分达阈值：歌名相等 +3 / 包含 +2、歌手出现在标题或 UP 主里 +3、
     时长 ≤1.5s +3 / ≤3s +2、带真实时间轴 +1（满分 10）；
   - 严格阈值 **7**（`BM_LYRICS_STRICT=1`，默认），放宽阈值 4（`BM_LYRICS_STRICT=0`）。

   不合格就当这一源没结果，**继续降级**——这是用户诉求「错的不该挡住下一级」的落点。
2. **时间轴真实校验 `looks_synced()`**：≥4 个带轴行、≥3 个不同非零时间、末行不早于时长 40%；
   替掉只看前 200 字有没有 `[mm:ss` 的旧判定（会被 `[00:00.000] 作词 : xxx` 与全零时间戳骗过）。
3. **元数据行清洗 `strip_meta_lines()`**：落库前剥掉 `作词/作曲/编曲/制作人/OP/SP…` 行；
   前端 `parseLrc` 也加一道（老库里已存的也能干净显示）。
4. **网易云默认关闭**（`BM_LYRICS_NETEASE=1` 才启用）；启用时改为**取得分最高**的一条，
   不再「第一个通过就返回」。
5. **候选不再截断**：去掉 `_from_netease` 的 `[:2]`，与 `_from_lrclib` 共用候选列表。

### 验证（真接口 + 用户真库）

| 曲目 | 修复前 | 修复后 |
|---|---|---|
| id 415 Rammstein《Ich tu dir weh》 | 网易云给 Jaymes Young《Infinity》 | **LRCLIB 正确 Rammstein 歌词**（`Nur für mich bist du am Leben…`，47 行） |
| id 32 周杰伦-晴天 | 无 | LRCLIB 带轴 44 行 |
| id 42 周杰伦-兰亭序 | 无 | LRCLIB 带轴 38 行 |
| id 345 电吉他教程 / id 435 Intro | 无 | 无（宁缺毋滥，符合预期） |

强制刷新后全库来源分布：`cc 32 / lrclib 2 / ai 1 / 空 2`，**ncm 归零**，库内已无元数据行残留。
测试：`tests/test_lyrics.py` 新增 18 条（候选顺序、元数据剥离、时间轴校验、置信度打分、
低置信度降级、网易云默认关闭），全套 **153 passed, 2 skipped**；`node --test tests/*.js` 24 passed。

**已知残留**：id 391「Sonne (3)」拿到了俄语翻唱版歌词（同名 + 时长巧合，歌手对不上但没触发硬门槛）。
要彻底挡住需要把歌手校验改成**必过**，代价是丢掉一批标题里没有歌手名的正确命中——暂不采用。

---

## BUG-008 合集/分P 标题的歌词候选提取把真歌名丢掉｜ P1 ｜ 已修（未打包）

**现象**：多P合集里的曲目（标题形如「合集标题 · 分P标题」）几乎永远拿不到外部歌词，
即使 LRCLIB 明明有——《兰亭序》《菊花台》《Sonne》都有带轴版本。

**根因**：`app/services/lyrics.py` → `title_candidates()` 先按 `[|｜/·◆★\-—–\s]+` 切段，
合集标题被切成一堆噪声段（「50首精选合集」「后台播放」「无损音质」…），
真正的歌名排在最后，而函数末尾 `return out[:4]` 恰好把它截掉。实测：

```
title_candidates("【周杰伦】50首精选合集/后台播放/无损音质/HIFI音质/华语流行音乐才是最叼的 · 周杰伦-兰亭序")
-> ['50首精选合集 后台播放 无损音质 HIFI音质 华语流行音乐才是最叼的 周杰伦 兰亭序',
    '50首精选合集', '后台播放', '无损音质']          # ← "兰亭序" 被 [:4] 截掉
```

`_from_netease` 更是只取 `title_candidates(title)[:2]`，连第三段都够不到。

**实测影响**（用户曲库抽样 121 首，其中 33 首无 CC 字幕）：

| 候选策略 | LRCLIB 命中 |
|---|---|
| 现状（`title_candidates`） | **0 / 33** |
| 优先取「 · 」后的分P标题 | **20 / 33（全部带轴）** |

命中的 20 条里 18 条明显正确（周杰伦 10 首时长几乎完全吻合：兰亭序 254/254、
听妈妈的话 266/266、菊花台 294/294、彩虹 264/264；Rammstein 8 首），
2 条同名撞车（`Sonne (3)` → `Schwarze Sonne / RAF 3.0`；巡演视频 → `No Strings Attached / 'N Sync`）。

**修复方向**：

1. `title_candidates()` 识别「 · 」结构：**分P标题优先**成候选，主标题段降到后面；
2. 候选上限 4 太小 → 提高，或按「像歌名的程度」排序；
3. `_from_netease` 的 `[:2]` 与 `_from_lrclib` 的 `[:4]` 统一走同一套候选排序；
4. 配合 BUG-007 的歌手/歌名复核，处理上面 2 条同名撞车。

**与 BUG-007 的关系**：两者叠加才是「网易云错配」的完整图景——候选错误让搜索引擎搜到
不相干的歌，接受门槛过松又让它被直接采纳。

### 修复（未打包）

`title_candidates()` 先按 ` · ` 拆标题，**分P 标题的候选排在最前**，再补整串标题的候选；
上限从 4 提到 6。清洗逻辑抽成 `_candidates_from()` 复用，原有单段标题行为不变。

**验证**：id 42《周杰伦-兰亭序》候选从
`['50首精选合集 后台播放 无损音质 HIFI音质 … 周杰伦 兰亭序', '50首精选合集', '后台播放', '无损音质']`
变成 `['周杰伦 兰亭序', '周杰伦', '兰亭序']`，LRCLIB 立刻命中带轴歌词（38 行）；
id 415《Ich tu dir weh》候选 `['Ich tu dir weh', 'Ich', 'tu']`，同样命中。

---

## 附：歌词来源覆盖率实测（2026-09-09，用户账号 362884497）

抽样 121 首（21 首已收藏 + 100 首随机），实际探测 120 首（1 首接口失败），
用真实 cookie 调 `/x/player/wbi/v2` 统计字幕轨：

| 来源策略 | 覆盖 | 备注 |
|---|---|---|
| 仅 B 站人工 CC 字幕 | **87 / 120 = 72.5%** | 已收藏曲目 **20/20 = 100%**；未收藏（多P子曲目）67/100 = 67% |
| CC + AI 字幕 | 90 / 120 = 75% | 只多 2.5pp，且 AI 字幕质量差（见 BUG-007） |
| CC + LRCLIB（**修 BUG-008 候选后**） | **107 / 120 = 89.2%** | 20 条命中里 2 条同名撞车 → 需歌手复核 |
| 现状全链路（含网易云） | — | 库里唯一一条 ncm 结果就是错的（BUG-007） |

无 CC 字幕的 33 首集中在两个合集：`【教程合集】德国战车Rammstein电吉他教程`（17 首，教学视频，
歌词本就无意义）与 `【周杰伦】50首精选合集`（11 首）。**结论**：B 站字幕天然与视频绑定、
不可能错配，但覆盖率 72.5%；把 LRCLIB 的候选修好可以补到 89%，代价是必须加置信度校验。

---

## BUG-009 多P合集子曲目标题重复拼接合集名（专辑页每行一长串）｜ P2 ｜ 已修（未打包）

**现象**（用户截图）：专辑页每行曲目名都是
`【周杰伦】50首精选合集/后台播放/无损音质/HIFI音质/华语流行音乐才是最叼的 · 001.周杰伦-晴天`，
一长串挤掉列宽、读不出歌名；播放器/队列/曲库列表同样受影响。

**根因**：`app/services/importer.py` 的 `part_display_title()` 有意把主标题拼在前面
（`return f"{main} · {cleaned}"`），子曲目标题按此入库，所有展示位直接用 `song.title`。

**修复**：

1. 新增 `app/services/titles.py`：`clean_part_title()` / `is_meaningful_part()` /
   `part_display_title()` / `short_title()`——子曲目标题**只保留分P名（歌名）**，
   占位名（`P1` / `正片` / 空）才回退主标题；单字中文歌名（`枫`）算有效歌名，
   不按长度一刀切。
2. `importer.py` 改用新 helper（单视频带分P、多P专辑子曲目两处构造点）。
3. `db/session.py` 新增**幂等**迁移 `_shorten_album_child_titles()`：把存量
   「合集标题 · 分P标题」收敛为分P标题。只在确实知道 `album.title` 时收敛，
   **不误伤**标题本身带 ` · ` 的曲目（例如 UP 主原标题
   `黄小琥《没那么简单》 但是方大同风格编曲 · 没那么简单` 保持原样）。

**验证**：用户库 518 条待收敛 → **0 条**；专辑 2 的曲目从
`【周杰伦】… · 001.周杰伦-晴天` 变为 `周杰伦-晴天`、`周杰伦-夜曲`…
`pytest` **156 passed, 2 skipped**。

---

## FEAT-002 手机端底部悬浮组：导航条 + 播放条尺寸统一 + 液态玻璃选中块｜ 已改（未打包）

**诉求演进**（用户逐轮反馈）：选中块不要粉色渐变 → 要跟手拖动 + 水珠形变 → 圆角/间距要与导航条
内缘对齐 → 形变更夸张 → 弹簧更软、判定更松 → 落位放慢不弹 → 导航条要更胖更短 →
形变要变成竖椭圆并可**溢出**条子 → 上方播放条也要改成与导航条同等大小。

**最终规格**（`app/web/static/style.css` 手机媒体查询 + `app/web/static/nav-liquid.js`）：

| 项 | 值 |
|---|---|
| 悬浮组公共变量 | `--nav-pad:6px` · `--nav-tabw:78px` · `--nav-gap:10px` · `--nav-orb:64px` · `--nav-h:66px` |
| 导航条 | 内容宽度（248px）居中，高 66px，圆角 999px |
| 播放条 | 宽 = 导航条 + 间距 + 圆钮 = **322px**，高 **66px**，与导航条左边缘对齐（x=89） |
| 圆钮 | 64px |
| 选中块（静止） | 78 × 52，四周留白 6px，与导航条内缘**同心**（圆心 x=67 完全重合） |
| 选中块（拖动最大） | 0.44 / 1.42 → **34 × 74px**，上下各**溢出条子约 4px**（水珠挤出效果） |
| 长按 | 0.69 / 1.23 |
| 弹簧 | 拖动中 k=0.24 / d=0.42（滞后量=水珠形变来源）；松手/切换 k=0.14 / d=0.84（过阻尼，零过冲，整格 512ms） |
| 吸附 | 当前格 +0.15 格惩罚（走 35% 就切）；松手速度 vel×0.25 限幅 ±5 |
| 窄屏（≤370px） | `--nav-tabw:66px` · `--nav-orb:58px` · `--nav-h:62px` |

**验证**（无头 Chrome 跑真实 CSS/JS，模拟 pointer 事件）：
静止 78×52 / 留白 6px / 圆心同心；拖动 0.440 / 1.420；松手吸附零过冲；程序切换 512ms；
播放条与导航组同为 322px 宽、66px 高、左边缘对齐、间隙 10px；
像素复核选中块溢出条子上下各约 4px。`pytest` 156 passed，`node --test` 24 passed。

**踩坑**：`.m-tab{flex:1 1 78px}` 时 Chrome 把容器 intrinsic 宽度算成内容宽度（84px），
必须写 `flex:0 0 var(--nav-tabw)`；`.m-tabs` 还要加 `padding` 让 tab 盒与选中块完全重合，
否则最左/最右格圆角与容器内缘不同心。

---

## FEAT-003 分享能力：正向系统分享面板 + 反向「分享到 BiliMusic」｜ 已实现（Web/桌面已验证，Android 待打包）

**诉求**：台账 #25（用户点单）——分享不该只有「复制链接」一种形态；反过来，在 B 站 App 点分享
时希望系统面板里能选 BiliMusic，一步入库。

**改动**（详见 `docs/issue-ledger.md` §4.5.1）：

| 层 | 落点 |
|---|---|
| 新增 | `app/core/share_links.py`（纯函数拼链接，缺字段返回空串）、`app/web/static/share.js`（`__share` / `__copyText` / `__receiveShare`） |
| 后端 | `Album.mid` + 幂等迁移；`_album_out` 下发 `mid`/`shareUrl`；`GET /api/albums/{id}/share-link`（系列缺 mid 用任一曲目 bvid 反查 UP 回填）；`/api/imports/batch` 的解析失败改 400 可读文案 |
| 前端 | 单曲 ··· 菜单 / 歌单详情 / 专辑合集详情 / UP 主页 / 导出弹窗 五处入口统一走降级链：原生桥 → `navigator.share` → 剪贴板 → `execCommand` → 弹窗展示 |
| Android | 桥 `shareText(title,text,url)`；`SEND text/plain` intent-filter + `launchMode=singleTop`；`EXTRA_TEXT` → 页面就绪后 `__receiveShare()` |

**顺带修掉的真 bug**：系列合集（`source_bvid = "sid:777"`）此前会被前端当成 bvid，拼出
`https://www.bilibili.com/video/sid%3A777` 这种打不开的链接——现在系列一律走
`space.bilibili.com/{mid}/channel/collectiondetail?sid=777`，拿不到 mid 就明确报「拿不到合集来源 UP」。

**验证**：`tests/test_share.py` 7 例 + `tests/test_share.js` 13 例；无头 Chrome 驱动真实页面
（单曲/专辑/歌单三处分享拿到的都是真链接；垃圾文本、未登录、全链路失败三种异常路径都有可读反馈）；
`pytest` 163 passed / 2 skipped，`node --test` 35 passed。Android 侧代码就绪，**需重新打包 APK 后真机验收**。

---

## BUG-010 手机端 5 项体验问题（UP 名不可点 / UP 页列数 / 搜索被删空即关 / UP 页搜不出结果 / 结果被输入法盖住）｜ 已修（未打包）

**来源**：用户真机实测（2026-09-10，3 张截图 + 文字描述）。逐条根因与修复：

| # | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | 音乐详情页点 UP 名不进 UP 主页 | 绑定只写了 `#lyrics-artist`（歌词页顶栏）与 `#player-artist`（播放条），漏了手机播放页大歌名下的 `#ly-artist-big` | `v3.js` 三处一起挂 `upFromPlayer`；CSS 加 `cursor:pointer` 与按下变色 |
| 2 | UP 主页「全部音乐」手机上列数过多（宽屏 4 列） | `.up-videos` 用 `repeat(auto-fill,minmax(150px,1fr))`，≥640px 可用宽度就塞进 4 列 | 手机媒体查询改成 `repeat(2,minmax(0,220px))`：恒定两列，列宽封顶 220px（宽屏也不会把封面撑成大图） |
| 3 | 搜索框删到最后一个字就关闭，得重新点圆钮 | `scheduleSearch()` 里 `if (!q) close()`——空串直接把手机搜索态整个收掉 | 空串只清结果（给一句提示占位），手机端保持面板与焦点；桌面端输入框常驻，仍沿用原来「收起结果面板」的行为 |
| 4 | UP 主页里唤起搜索看不到结果 | `.search-drop` 的 `z-index:75` **低于** `#up-panel` 的 90，下拉被作品页盖住（胶囊 z=110 能看见，结果看不见） | 下拉提到 `z-index:120`（同时高于歌词页 80），手机端胶囊提到 130 |
| 5 | 搜索结果被输入法盖住 | 旧逻辑只在 `visualViewport.resize` 时给**已存在**的面板写行内 `bottom`；结果面板要等接口返回才出现，早错过了那次事件 | 改成把键盘占位高度写成 CSS 变量 `--kb`，胶囊与结果面板的 `bottom` / `max-height` 都带 `var(--kb)`；`resize`/`scroll`/`orientationchange` 都刷新 |

**验证**（无头 Chrome + CDP 设备仿真，直接量真实 DOM；脚本 `/tmp/probe_bugs.py`、`/tmp/probe_kb.py`）：

- 390×844：orbSearch → 删空后 `#topbar.searching=true`、面板仍开、输入框仍聚焦（原为 false/关掉）。
- 390×844：UP 面板打开后 `.search-drop` 中心点命中 `DIV.sd-sec`（修前命中 `IMG`＝被面板盖住）；`.m-orb` 中心命中 svg，可点。
- 390×844：点 `#ly-artist-big` → `#up-panel` 打开（修前 hidden 仍为 true）。
- 768×1024（平板/窄桌面）：`.up-videos` 列模板 `220px 220px`，封面 220×124（修前 4×164×92）。
- 模拟键盘 300px（`--kb:300px`）：胶囊 bottom 由 826 → 526、结果面板 bottom 由 720 → 456，均在可视底 544 之上；桌面 1280px 行为不变（清空仍收起面板、输入框保持聚焦）。
- 回归：`pytest` 163 passed / 2 skipped；`node --test tests/*.js` 37 passed。

---

## BUG-011 UP 主页「加载更多」滚到底仍被播放条压住｜ 已修（未打包）

**现象**（用户截图）：UP 作品页滚到底，「加载更多」按钮下沿被底部播放条盖住约三分之一，且已经**滚不动了**。

**根因**：`.up-lay` 在手机端的下留白是写死的 `150px`，而播放条占位是 `bottom:96px + 高 66px = 顶部在 162px`——少了 12px，滚到底也差这一点。

实测（无头 Chrome 390×844，模拟播放条显示）：播放条 `top=682`，「加载更多」`bottom=694` → **重叠 12px**，`.up-scroll` 滚动余量已为 0。

**修复**：手机端 `.up-lay` 下留白改为 `calc(200px + var(--inset-bottom, env(safe-area-inset-bottom, 0px)))`
（162px 播放条 + 约 38px 呼吸位；带安全区机型自动再加底部安全区）。

**验证**：同条件复测 390 与 430 两种宽度——按钮 `bottom=644`、播放条 `top=682`，**重叠 0px**，滚到底可见。桌面端 `.up-lay` 下留白 150px 对 `bottom:18px + 56px` 的桌面播放条本就够用，未改动；其余页面走 `.app .main{padding-bottom:170px}`，不受影响。

---

## BUG-012 UP 作品页搜索「有结果、没搜索框」——层叠上下文陷阱｜ 已修（未打包）

**现象**（用户二次反馈 + 截图）：BUG-010 #4 把结果面板抬高后，UP 作品页里能搜出结果了，但**搜索框本身仍然看不见**。

**根因**：`.app .main{position:absolute;inset:0;z-index:0}` —— `.main` 带 z-index 就**自成层叠上下文**。
搜索胶囊 `#topbar` 是 `.main` 的子节点，它的 `z-index:130` 只在 `.main` 内部生效；对外的比较是
`.main(0)` vs `#up-panel(90)`，所以胶囊永远被作品页盖住。而结果面板 `.search-drop` 是 `.app` 的直接子节点，
抬高 z-index 就有效——这正是「结果看得见、输入框看不见」的原因。

**修复**：`v3.js` 新增 `window.__searchBarPortal(on)`：手机端进入搜索态时把胶囊临时挂到 `#app` 下
（此时它已是 `position:fixed`，视觉位置不变；`appendChild` 搬家不丢事件监听），关闭搜索时放回 `.main` 首位。
桌面端完全不走这条路径（桌面 `.topbar` 是 sticky 常驻，不进搜索态分支）。

**验证**（无头 Chrome + CDP，390×844，`document.elementFromPoint` 命中测试——浏览器判断"谁在最上层"的权威手段）：

| 场景 | 胶囊中心命中 | 结论 |
|---|---|---|
| UP 作品页 + 搜索态（修复后，胶囊挂 `#app`） | `input#search` | 可见可点 ✅ |
| UP 作品页 + 搜索态（把胶囊放回 `.main` 复现旧行为） | `div#m-dock` | 被盖住 ❌ |
| 歌词/播放页 + 搜索态 | `input#search` | 可见可点 ✅ |
| 桌面 1280：搜索、清空全程 | 父节点始终 `mainEl`，行为不变 | 无回归 ✅ |

结果面板中心命中 `div.sd-sec` ✅；关闭搜索后胶囊父节点回到 `mainEl`、`searching` 类已移除 ✅。

---

## BUG-013 手机端搜索框与播放条不齐平（顺带揪出重复的 .search-drop 规则）｜ 已修（未打包）

**现象**（用户截图）：手机底部搜索胶囊与上方播放条左右都不对齐——胶囊左边缘更靠左、右边缘更靠右（数量上：胶囊 292px 宽，播放条 340px 宽）。

**根因**：两条各写各的宽度。播放条是 `left:12/right:12 + width:calc(--nav-tabw*3 + …) + margin-inline:auto`（居中，390px 下占 25→365）；
搜索胶囊却是 `left:12px; right:86px`（占 12→304）。另外发现手机媒体查询里 `.search-drop` **被定义了两次**，
后一条（`top:76px;left:12px;right:12px;width:auto;max-height:calc(100% - 200px)`）把前面带 `var(--kb)` 的
`max-height` 一起盖掉了——BUG-010 #5 的键盘避让其实一直被它削弱。

**修复**（`app/web/static/style.css`）：

- `.topbar.searching` 与 `.search-drop` 都改成与播放条同一条公式 + `margin-inline:auto` + `max-width:calc(100% - 24px)`；
- 删掉手机块里重复的那条 `.search-drop` 规则，让带 `--kb` 的那份成为唯一来源。

**验证**（无头 Chrome + CDP 实测矩形）：

| 视口 | 播放条 | 搜索胶囊 | 结果面板 |
|---|---|---|---|
| 390 | 25→365（340） | 25→365（340）✅ | 25→365（340）✅ |
| 360 | 40→320（280） | 40→320（280）✅ | 40→320（280）✅ |

键盘避让复测（390，--kb=300px）：胶囊 766→466、面板底 756→456（可视底 544），面板 max-height 584→284；
UP 作品页里胶囊中心仍命中 `input#search`，关闭后回归 `.main`。

---

## BUG-014 搜索结果面板与播放条的上下顺序反了｜ 已修（未打包）

**现象**（用户截图）：手机搜索时，结果面板跑到**播放条上方**；用户要的顺序是「播放条 → 结果面板 → 搜索胶囊」。

**根因**：面板原来写死躲播放条——`body:has(#player-bar:not(.hidden)) .search-drop{bottom:172px}`
（172 = 播放条底距 96 + 高 66 + 10），于是三者从下往上成了「胶囊 → 播放条 → 面板」。面板高度是动态的，
写死偏移也顶不准。

**修复**：

- `style.css`：删掉那条「面板躲播放条」的规则，面板恒定贴在胶囊上方（`bottom:88px + 安全区 + --kb`）；
  高度预算改成 `max(140px, calc(100% - 320px - var(--kb)))`——给「播放条 66 + 两个 10px 间隙 + 顶部留白」预留位置，
  这样播放条被顶上去后稳定停在上方 ~156px，不会贴到状态栏；`#player-bar` 加 `transition:bottom .26s`（顶上去时有动画）。
- `v3.js`：新增 `liftPlayerBar()`，用**实测 rect** 反推播放条底距（`视口高 − 面板顶 + 10`），
  在 `place()` / `ResizeObserver(面板)` / 键盘同步 / 关闭搜索时调用；关闭时把行内 `bottom` 清掉复位。
  面板高度 < 180px 的退化场景（矮屏 + 键盘）不顶，避免把播放条推到状态栏上。

**验证**（无头 Chrome + CDP 实测矩形）：

| 场景 | 播放条 | 结果面板 | 搜索胶囊 | 间隙 |
|---|---|---|---|---|
| 390×844 | 156→222 | 232→756 | 766→826 | 10 / 10 ✅ |
| 360×640 + 键盘 300px | 未顶（留在原位，被键盘盖住＝不可见） | 112→252（140px 兜底） | 262→322 | 面板仍在胶囊上方 ✅ |
| 关闭搜索后 | 复位 682→748（CSS 默认） | — | — | ✅ |

---

## BUG-015 导航条选中块「点击即变形」，没有过渡｜ 已改（未打包）

**现象**（用户）：点底部导航切 Tab 时，玻璃块的**形变是一帧到位的**（突然变胖变圆），希望 ~0.3 秒缓慢变形。

**根因**：位置走弹簧（`K_SETTLE/D_SETTLE`，≈400ms 落位），但**形变系数直接写进 CSS 变量**：
`onDown` 里 `press = 0.55` 后立刻 `paint()`，`--pill-sx/--pill-sy` 当帧就是最终值；切换 Tab 时
`t = |target − pos| / (span × 0.5)` 也会瞬间跳到 1，形变同样是一帧到位。

**改动**（`app/web/static/nav-liquid.js`）：给形变加时间常数缓动 `SHAPE_TAU = 0.15s`
（`shapeSx/shapeSy += (want − 当前) × (1 − e^(−dt/τ))`），并让 rAF 循环在「位置已停但形变还在缓动」时继续跑帧，
否则形变会卡在半路。首屏/旋转屏/`prefers-reduced-motion` 仍走 `snap` 立刻到位。

**验证**（无头 Chrome + CDP 采样 `--pill-sy`，390×844）：

| 场景 | 采样结果 |
|---|---|
| 点「资料库」Tab | 25ms 时 1.146 → 111ms 峰值 1.245 → 之后 ~0.7s 平滑落回 1.000（改前：首帧即到位） |
| 模拟拖动（每 30ms 移 22px） | 30ms 1.092 → 300ms 1.298，随滞后量平滑上升（拖动的手感与幅度未被削弱） |

想更慢/更快改 `SHAPE_TAU` 一个常量即可（0.15 ≈ 90% 用 0.35s）。

---

## BUG-016 PC 歌曲详情页左侧排版与手机版不统一｜ 已改（未打包）

**现象**（用户两张截图对照）：PC 详情页左列各行宽度各行其是——封面 300px、歌名行 498px、进度 498px、
控制行 372px（还多出「播放模式/＋」两个键）、底行几乎空（歌词气泡被桌面规则隐藏、播放列表键没有样式 → 实测 **0×17 不可见**）。
手机上则是一条 75% 宽的公共列，五行一起对齐。

**改动**（`style.css` + `base.html`）：

| 项 | 改前（PC） | 改后（PC） |
|---|---|---|
| 列宽 | 封面 300 / 信息行 498 / 进度 498 | 统一 `--ly-desktop-col: min(360px, 30vw)`（封面/信息行/进度/控制行/底行共用） |
| 控制行 | 播放模式·上一首·暂停·下一首·＋（居中，5 键） | 上一首·暂停·下一首 + `justify-content:space-between`（与手机同一排版） |
| 底行 | 歌词气泡隐藏、播放列表键 0 宽不可见 | `justify-content:space-between`：左「播放模式」、右「播放列表」（各 44px），与手机同一条底行 |
| ＋ 添加至歌单 | 左列控制行 | 收起（··· 菜单里仍有「加入歌单」，曲目行也有） |

`#ly-mode` 从 `.ly-ctl` 移到 `.ly-foot` 首位（DOM 挪位，事件监听按 id 绑定不受影响），
桌面端显示、手机端隐藏（手机这一格是歌词气泡）。手机端排版与数值**完全未变**（复测一致）。

**验证**（无头 Chrome + CDP 量 rect）：

| 视口 | 封面 | 信息行 | 进度 | 控制行 | 底行 |
|---|---|---|---|---|---|
| 1280 PC | 89→449（360） | 89→449 | 89→449 | prev 89 / play 247 / next 405（两端对齐） | 模式 89 / 列表 405 |
| 1024 PC | 61→368（307） | 61→368 | 61→368 | prev 61 / play 193 / next 325 | 模式 61 / 列表 325 |
| 390 手机 | 49→342（293） | 49→342 | 49→342 | prev 49 / play 167 / next 285 | 歌词 49 / 列表 289（未变） |

---

## BUG-017 手机端账号页没有「退出登录」｜ 已补（未打包）

**现象**（用户截图）：手机端账号页只有「同步 B 站收藏夹 / 导出歌单链接 / 外观 · 亮暗」三行，
**没有退出登录**——它原来只存在于桌面侧栏的用户弹层（`base.html:109` 的 `#side-logout`），而侧栏在手机上本来就是隐藏的。
顺带发现 `export.js` 里那段 `#btn-logout` 的登出逻辑是**死代码**（模板里根本没有这个 id）。

**改动**：

- `library.html`：账号页 `登录态` 下补一行「⏻ 退出登录 / 清除本机凭据」（`.acct-row.danger`，红色）。
- `style.css`：新增 `.acct-row.danger{color:#f87171}` + hover 底色（与侧栏 `.up-row.danger` 同色系）。
- `v3.js`：新增 `window.logoutNow()`——走 `__confirmModal` 二次确认（Android WebView 里 `confirm()` 不可靠，项目统一用自绘弹窗），
  确认后 `POST /api/auth/logout` 并回首页；桌面侧栏的退出登录也改用它（两处行为一致）。

**验证**（无头 Chrome + CDP，390/1280，**只点「取消」不真的退**）：

| 场景 | 结果 |
|---|---|
| 手机账号页 | 四行：同步 / 导出 / 外观 / 退出登录（rgb(248,113,113)）✅ |
| 点退出登录 | 弹出二次确认「退出登录 · 本机保存的 B 站凭据会被清除…」✅ |
| 点取消 | 弹窗关闭、`data-auth` 仍为 1（没有误退）✅ |
| 桌面侧栏 | `#side-logout` 仍可用，且同样走二次确认 ✅ |

---

## BUG-018 账号页行尾解释性小字（像半成品）｜ 已去掉（未打包）

**现象**（用户截图 + 「后面的解释语句都不要，显得像一个半成品」）：账号页每行右侧都挂一句灰色说明，
底部还有一句独立说明，读起来像开发占位文案。

**去掉的内容**（`library.html`）：

| 行 | 去掉的小字 |
|---|---|
| 登录 B 站账号 | 扫码 · 凭据存本机 |
| 同步 B 站收藏夹 | 曲库事实源在 B 站 |
| 导出歌单链接 | bilimusic 夹 |
| 外观 · 亮 / 暗 | 当前 · 浅色（`#theme-mode-sub`） |
| 退出登录 | 清除本机凭据 |
| 页脚 | BiliMusic · 曲库事实源在 B 站侧 |

`v3.js` 里给 `#theme-mode-sub` 赋值的代码本来就带 `if (sub)` 守卫，元素删掉后不会报错（已实测无 JS 异常）。

**验证**：无头 Chrome + CDP 读 `#view-account` 的 `innerText` —— 现在是
`凝眸-无限意 / 已登录 · 音质最高 192K / 同步 B 站收藏夹 / 导出歌单链接 / 外观 · 亮 / 暗 / 退出登录`，
`.sub` 元素计数 0，页脚说明句已不存在。

---

## BUG-019 PC「UP 主视图」右侧列表重排：只留歌名 + 收藏｜ 已改（未打包）

**现象**（用户截图）：PC 资料库 → UP 主视图，右侧选中 UP 的歌曲列表沿用曲库那套宽行
（序号 · 封面 · 标题 · 歌手 · 歌单胶囊 · 音质 · 时长，且没有收藏按钮）。用户要求：「右边重新排，只需要歌名和收藏按钮」。

**改动**：

| 层 | 内容 |
|---|---|
| `routes.py` | `/partials/songs` 增加 `compact` 参数并传进模板 |
| `partials/songs.html` | 新增紧凑行分支：`trk-mini` = 封面 + 歌名 + `★`（`data-collect` / `data-uncollect`，复用已有的 document 级委托） |
| `style.css` | `.trk.trk-mini` / `.list-head-mini` 三列网格 `44px 1fr 44px`；星形默认灰、已收藏粉色 |
| `v3.js` | UP 视图的表头与请求按宽度分流：**仅 ≥901px** 用 `list-head-mini` + `&compact=1`，手机端维持原样 |

**验证**（无头 Chrome + CDP）：

| 视口 | 右侧列表首行 | 网格 | 表头 |
|---|---|---|---|
| 1440 PC | `trk card trk-mini`：封面 + 歌名 + ★ | `44px 646px 44px` | 「歌名 / 收藏」 |
| 390 手机 | 原样（`no/eqbox/im/tt/pl-pill/q/du` 全在） | `36px 209px 76px` | 原 7 列（手机端本就不显示） |

星形按钮点击已用拦截 `fetch` 的方式验证：发出的是 `POST /api/songs/258/uncollect`（未真的改数据）。
读图复核确认：每行 = 封面 · 歌名 · 粉色星形，表头「歌名｜收藏」。

---

## BUG-020 播放条星星「自动收藏」：收藏状态只按 bvid 记，同一多分 P 视频各分集互相串台｜ 已修（未打包）

**现象**（用户截图）：只是收听了「张震岳合集」这首《爱我别走》，从没点过收藏，播放条的星却是**粉色（已收藏）**；
而专辑列表里这首歌那行的星是灰的。用户：「实际上收藏需要点击界面后面的星」。

**根因**：`v3.js` 的收藏表只按 `bvid` 记：

```js
collected[s.bvid] = 1;          // loadCollected()
var on = !!collected[currentBvid()];
```

而《爱的初体验》(cid 1405147297，已收藏) 与《爱我别走》(cid 1405147389，未收藏) **是同一个视频 BV1yN4y1H7XX 的两个分 P**——
bvid 相同，于是播放后者时也命中了收藏表，星星被点亮。**收藏的粒度其实是 (bvid, cid)**，多分 P 里每个分集是独立歌曲。

**修复**：

| 改动 | 内容 |
|---|---|
| `api/routes.py` | `song_out` 补 `cid`（前端需要按 bvid+cid 判状态） |
| `v3.js` | 收藏表键改为 `bvid:cid`：`songKey()` / `currentRef()` / `currentKey()`；`loadCollected` 以服务端 `collected` 为准；`__markCollected` 仍支持「整视频收藏」（发现页卡片） |
| `v3.js` | 星星点击：当前歌在曲库里有行时走 `POST /api/songs/{id}/collect`（**精确认领这一首**，多分 P 只收藏当前分集）；无行（试听流）才回退整视频导入 |

**验证**（无头 Chrome + CDP，真页面 + 真接口）：

| 场景 | 结果 |
|---|---|
| 播《爱我别走》(collected=0，与已收藏的《爱的初体验》同 bvid) | 星 **不亮** ✅（修前为亮） |
| 播《爱的初体验》(collected=1) | 星亮 ✅ |
| 点星 | `POST /api/songs/10/collect` → 星亮 + toast「已收藏 · 已加入我的曲库」+ 该曲进入 `/api/songs` ✅ |

> ⚠️ **记录一次测试事故（已复原）**：验证「取消收藏」分支时脚本自动点了确认框，触发了 #33 的
> 「多分 P 专辑 → 取消整张收藏」流程，把该专辑本地 60 首连 B 站收藏一起删了。
> 已用 `POST /api/imports` 重新导入恢复：专辑（现 id=5，60/60 分 P）、曲库行数 277、
> 已收藏 19 首（含原本单独收藏的《爱的初体验》《嗨嗨人生》，`fav_folder_id=4013781715` 说明 B 站收藏也已恢复）——与本轮开始前的库状态一致。
> **顺带提醒**：合集子作品点「已收藏的星」会走「取消整张专辑」这条重路径，容易误伤；建议改成对子作品只做本地「移出曲库」。

---

## BUG-021 真机播放卡顿（锁屏/后台/切前台尤甚）｜ 已修（Android 部分待打包验证）

**现象**（用户真机）：播放会卡顿；锁屏解锁、后台播放、切到其他 App 再回来时尤其明显。用户另附了一份外部
优化方案（docx）——逐条核对代码后，结论如下（**它的 P0 项其实早已实现**）：

| 方案里的说法 | 核对结果 | 证据 |
|---|---|---|
| ① 首次播放要等整文件下载完（应改流式代理） | ❌ **不成立**：现在就是流式代理 | `/api/stream/{bvid}?cid=` 直接把 CDN 流转发（`upstream.aiter_bytes(64KB)`），支持 Range/206，不落盘；`files.py::_download_segmented` 是旧的入库下载路径，在线版播歌不走它 |
| ② CDN 多源 Fallback 缺失 | ✅ 成立，已修 | `AudioStream` 有 `backup_urls`，但流式路由只用 `base_url`（备用源仅旧下载路径在用） |
| ③ httpx 连接未复用 | ❌ 不成立 | `BiliClient.http` 是实例级单例 `AsyncClient`（默认连接池 100/20），路由全程复用它 |
| ④ 前端无预缓冲 | ✅ 部分成立，已修 | 主 `<audio>` 原为 `preload="none"`，且没有任何 `waiting/stalled` 恢复 |
| ⑤ 重试太简单（`_SEG_RETRIES`） | ⚠️ 位置过时 | 那段属旧下载路径；流式路径此前**完全没有**重试 |

**真正对应症状的根因**：Android 侧 `MediaPlaybackService` 只有前台服务 + MediaSession，
**没有 WakeLock / WifiLock**——息屏后系统降频/进入省电，CPU 与 Wi-Fi 同时打盹，流式音频随即断供，
解锁回来就是一段卡顿。这是「锁屏/后台/切前台才卡」的典型成因。

**改动**：

| 层 | 内容 |
|---|---|
| Android | `MediaPlaybackService` 播放期间持有 `PARTIAL_WAKE_LOCK` + `WIFI_MODE_FULL_HIGH_PERF`，暂停/停止/销毁即释放；`AndroidManifest` 加 `WAKE_LOCK` 权限 |
| 后端 | `/api/stream` 按 `candidate_urls()`（base + backup CDN）换源重试，连接层失败或 4xx/5xx 都换下一个镜像，每次之间 0.3s 递增退避；全部失败才 502（文案说明「已尝试全部镜像」）；只在首字节之前换源，开始推流后不切 |
| 前端 | 主 `<audio>` 改 `preload="auto"`；新增卡顿自愈：`waiting/stalled/error` 后若 3 秒仍未恢复（`readyState<3` 且未暂停），在当前进度重开一次，15 秒冷却、连续 5 次上限，正常 `playing` 即清零 |
| 未采纳 | 换 howler.js / hls.js（会丢掉现役双轨交叉淡入与歌词联动，且 B 站给的是 m4s 直链，不是 HLS）；`Cache-Control: max-age=86400`（对 Range 代理有害，中间件已特意不给 `/api/stream` 加 no-store） |

**验证**：

- 后端换源：新增 `tests/test_stream_fallback.py` 5 例（首选正常不换、连接失败换源、5xx 换源、全挂才 502、Range 头透传）→ 全过。
- 前端自愈：无头 Chrome + CDP 实测——停住 3s 重开 1 次、15s 冷却期内不重复、`readyState=4` 健康不乱动、暂停态不重开；`preload` 读到 `auto`。
- Android WakeLock 需重新打包后在真机验证（代码已就位）。
- 回归：`pytest` 168 passed / 2 skipped；`node --test` 37 passed。


---

## BUG-022 后端启动时 B 站网络抖动 → 账号被降级为「未登录」｜ 已修（未打包）

**现象**：本地后端重启时若那一刻到 B 站（`api.bilibili.com`）的请求抖动，启动日志出现
`恢复登录失败，需重新登录（RemoteProtocolError）`，之后所有 `/api/*` 直接 401——**必须再重启一次**才恢复。
实测遇到两次（都在验证 #23 时触发重启），`cookies.json` 里的 `SESSDATA` 一直完好，属于「凭据有效但没接上」。

**根因**：`app/main.py` 的 lifespan 里，登录恢复 `accounts.activate()` 是**一次性的**——任何异常都直接落到
`dbs.reset_to_pending()`，把运行时切成未登录态；网络类错误（`httpx.RemoteProtocolError` 等）与
「凭据失效」被同等对待。

**修复**：网络类错误（`httpx.HTTPError` / `OSError` / `ConnectionError`）重试 2 次（1.5s、3s 退避），
凭据失效（`BiliApiError`）不重试、直接按需重新登录；全部失败才 reset 到 pending。

**验证**：重启后端 6 次（含一次真实抖动：日志出现重试记录后仍成功恢复），`/api/songs` 稳定 200，
未再出现「被登出」。
